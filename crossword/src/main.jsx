import React from "react";
import ReactDOM from "react-dom/client";
import EmbedCrosshare from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
    <React.StrictMode>
        <EmbedCrosshare />
    </React.StrictMode>
);
